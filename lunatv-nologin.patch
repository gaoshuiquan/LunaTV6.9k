diff --git a/src/pages/login.tsx b/src/pages/login.tsx
index 3e1db6a..aaa9999 100644
--- a/src/pages/login.tsx
+++ b/src/pages/login.tsx
@@ -1,45 +1,28 @@
-import React, { useState } from "react";
-import { useRouter } from "next/router";
-import { login } from "@/utils/auth";
-
-export default function Login() {
-  const [email, setEmail] = useState("");
-  const [password, setPassword] = useState("");
-  const router = useRouter();
-
-  const handleSubmit = async (e: React.FormEvent) => {
-    e.preventDefault();
-    const result = await login(email, password);
-    if (result.success) {
-      router.push("/");
-    } else {
-      alert("Login failed");
-    }
-  };
-
-  return (
-    <div className="login-page">
-      <h2>Login</h2>
-      <form onSubmit={handleSubmit}>
-        <input
-          type="text"
-          placeholder="Email"
-          value={email}
-          onChange={(e) => setEmail(e.target.value)}
-        />
-        <input
-          type="password"
-          placeholder="Password"
-          value={password}
-          onChange={(e) => setPassword(e.target.value)}
-        />
-        <button type="submit">Login</button>
-      </form>
-    </div>
-  );
-}
+/**
+ * LunaTV No-Login Version (Local LAN Mode)
+ * 直接绕过登录逻辑 → 自动写入固定 token → 自动跳首页
+ */
+
+import { useEffect } from "react";
+
+export default function Login() {
+  useEffect(() => {
+    if (typeof window !== "undefined") {
+      const token = localStorage.getItem("token");
+
+      if (!token) {
+        // 固定本地 token（仅限局域网自用）
+        const fakeToken = "local-home-user-token";
+        localStorage.setItem("token", fakeToken);
+      }
+
+      // 自动跳首页
+      window.location.href = "/";
+    }
+  }, []);
+
+  return null; // 页面不渲染任何内容
+}
diff --git a/src/middleware/auth.ts b/src/middleware/auth.ts
index 8d7f444..bbb8888 100644
--- a/src/middleware/auth.ts
+++ b/src/middleware/auth.ts
@@ -1,22 +1,19 @@
-import jwt from "jsonwebtoken";
-import { NextApiRequest, NextApiResponse } from "next";
-
-export function auth(req: NextApiRequest, res: NextApiResponse, next: () => void) {
-  const token = req.headers.authorization?.replace("Bearer ", "");
-
-  if (!token) {
-    return res.status(401).json({ message: "Unauthorized" });
-  }
-
-  try {
-    const decoded = jwt.verify(token, process.env.JWT_SECRET!);
-    (req as any).user = decoded;
-    next();
-  } catch (err) {
-    return res.status(401).json({ message: "Invalid token" });
-  }
-}
+/**
+ * LunaTV No-Auth Mode (Local LAN Only)
+ * 后端直接允许所有请求，无需 Token，无需验证
+ * 不建议公开到公网！
+ */
+
+export function auth(req: any, res: any, next: () => void) {
+  // 伪造一个默认本地用户
+  req.user = {
+    id: "home",
+    email: "local@home",
+    role: "admin",
+  };
+
+  next(); // 永远放行
+}
